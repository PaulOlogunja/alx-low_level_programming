#include "main.h"

/**
 * add - a function that adds two integers and returns the result.
 *
 * @int: Given numbers
 *
 * Return: the sum of the two numbers
 */
int add(int a, int b)
{
	int result = a + b;

	return (result);
}
